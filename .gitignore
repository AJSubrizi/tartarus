node_modules
# Ship dist so `npx github:AJSubrizi/tartarus mcp` works like other MCPs
# dist is committed; local rebuilds still overwrite
.DS_Store
*.local
.env
.env.*
data/
.tartarus-state.json
.tartarus/
.tartarus-smoke-ok
*.tsbuildinfo
.vite
release/
.electron-builder/
